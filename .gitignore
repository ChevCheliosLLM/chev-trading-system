# ─── CREDENTIALS — NEVER commit these ───────────────────────────────────────
google_credentials.json
*.json.key
.env
secrets.py
secrets.local

# ─── Python environments (huge, not needed) ──────────────────────────────────
.venv/
venv/
env/
chev_env/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# ─── Open WebUI full installation (too large, Kev installs this separately) ──
open-webui/

# ─── Personal trade / runtime data (each person has their own instance) ─────
performance.jsonl
chev_journal.json
chev_decisions.jsonl
jane_journal.json
labels_open.jsonl
labels_closed.jsonl
labels_discovery.json
daily_risk.json
system_state.jsonl
tunables.json
counterfactual_report.txt
weight_proposal_report.txt
weight_overrides.json
*.log

# ─── Not part of this project ────────────────────────────────────────────────
Ale/

# ─── Local backups / scratch ─────────────────────────────────────────────────
*.bak
*.bak-*

# ─── Third-party tools downloaded locally, not part of this project ─────────
scrcpy-win64-v4.0/
node_modules/

# ─── Binaries / executables ──────────────────────────────────────────────────
*.exe
cloudflared.exe

# ─── OS junk ────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ─── IDE settings ───────────────────────────────────────────────────────────
.vscode/
.idea/
*.suo
*.user
*.code-workspace
